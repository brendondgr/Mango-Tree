# Planner

**Placeholder — not implemented.** This package is empty and nothing imports it.

The intended role is broad reasoning, lightweight inspection, and delegation to
app specialists. Today the coordinator's `reason` node
(`utils/agents/coordinator/graph.py`) does all reasoning inline, and there are no
specialist graphs to delegate to.

Do not document planner behaviour as if it exists.
