# API Schemas

**Placeholder — no code here.** Request and response shapes are validated inside
each app's views and services. Agent-side schemas live in
`utils/agents/schemas/`; agent tool parameter schemas are declared in
`config/tools.yaml`.
