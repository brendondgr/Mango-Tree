from __future__ import annotations

# DTOs for the exercise app are defined in Stage 4 (port domain logic).
# They mirror the legacy Pydantic models (Workout, Exercise, Routine,
# Equipment, WorkoutLog) as plain dataclasses shared by services, the DRF
# API, and the agent tools.
