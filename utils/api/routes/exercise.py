"""Route module for the exercise app.

Reserved in Stage 2; real endpoints are registered in Stage 5 once the DRF
views over backend/services/ exist. Included in config/django/urls.py under
the ``/api/exercise/`` prefix.
"""

from django.urls import path

urlpatterns: list[path] = []
