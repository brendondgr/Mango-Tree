# Coordinator

LangGraph coordinator graph: classifies requests, selects routes, assembles scoped task packages, validates structured results, and records events.

See `docs/skills/repo-structure/structures/langgraph.md`.
