# Projects

**Placeholder — not implemented.** This directory contains only this file. There
is no backend, frontend, agent tool, API route, or workspace tab here.

Note that project management *is* implemented, in `utils/apps/projectmanager/`.
This directory is a leftover skeleton with a confusingly similar name.

To build something here, follow `docs/skills/app-modules/`.
